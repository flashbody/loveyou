---
description: LoveYou 位置与遗言功能规格
alwaysApply: true
---

# 位置系统

## 位置采集策略

- **采集频率**: 每 4 小时本地缓存一次位置
- **精度要求**: GPS 精确到楼栋级别
- **存储方式**: 纯本地存储，不上传服务端
- **仅存最近一次位置**，不保留历史轨迹（参考 Snug Safety）

## 位置发送

- 触发通知时，取最近一次缓存位置附加到邮件
- 位置以文字地址 + 地图链接形式呈现
- 如无位置权限或无缓存，邮件中注明"位置不可用"

## 隐私保护

- 位置数据绝不上传至服务器
- 仅在触发紧急通知时，由设备端直接嵌入邮件发送
- 用户可在设置中查看当前缓存的位置信息

---

# 遗言 / 留言功能

## 功能描述

- 用户可在 App 内编写一段**遗言/留言**
- 纯文本，存储在本地
- 无字数上限（建议 UI 提示不超过 2000 字）

## 触发发送

- 当系统向紧急联系人发送邮件时（Level 2 超时 / Level 3 SOS），**自动附带遗言内容**
- 遗言显示在邮件正文中，标记为"TA 想对你说的话"

## 编辑与更新

- 用户可随时编辑遗言，即时保存到本地
- 下次触发通知时使用最新版本
